<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inventario SAP B1</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <div class="alert alert-warning">
      <h3>⚠️ No hay datos de inventario cargados</h3>
      <p>La primera carga puede tardar varios minutos. Haz clic en el botón para comenzar.</p>
      <button onclick="cargarInventario()" class="btn btn-primary mt-3">Cargar Inventario Manualmente</button>
      <div id="status" class="mt-3"></div>
    </div>
    <a href="/" class="btn btn-outline-secondary mt-3">← Volver al inicio</a>
  </div>
  <script>
    function cargarInventario() {
      const btn = document.querySelector('button');
      const status = document.getElementById('status');
      btn.disabled = true;
      btn.innerHTML = 'Cargando...';
      status.innerHTML = '<div class="spinner-border text-primary" role="status"><span class="visually-hidden">Loading...</span></div>';
      fetch('/cargar-inventario-manual')
        .then(response => response.json())
        .then(data => {
          if (data.success) {
            status.innerHTML = '<div class="alert alert-success">✅ Inventario cargado correctamente. Recarga la página.</div>';
            setTimeout(() => {
              location.reload();
            }, 2000);
          } else {
            status.innerHTML = '<div class="alert alert-danger">❌ Error al cargar inventario: ' + data.error + '</div>';
            btn.disabled = false;
            btn.innerHTML = 'Cargar Inventario Manualmente';
          }
        })
        .catch(error => {
          status.innerHTML = '<div class="alert alert-danger">❌ Error de conexión</div>';
          btn.disabled = false;
          btn.innerHTML = 'Cargar Inventario Manualmente';
        });
    }
  </script>
</body>
</html>